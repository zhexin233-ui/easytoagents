# Agents 实机 smoke 记录（2026-09-12）

## 环境与范围

- 主机：macOS，仓库 `/Users/zhexin/github/easytoagents`。
- 本次只执行不会写入用户配置的版本、帮助和只读列表命令；没有创建临时 Agent，也没有修改 `~/.claude`、`~/.codex`、`~/.cursor`、`~/.zcode` 或 `~/.config/opencode`。
- 因此本记录区分“CLI 可达性探针”和“应用自动化替代验证”，不把帮助输出冒充为真实 Apply/Restore smoke。

## CLI 探针结果

| 工具 | 命令 | 结果 | 结论 |
| --- | --- | --- | --- |
| Claude Code | `claude --version` | `2.1.269 (Claude Code)` | CLI 可执行 |
| Claude Code | `claude agents --json` | 返回当前后台/交互会话清单（本机当时 1 个 idle 会话） | 该只读子命令用于会话浏览，不验证 Agents 目录文件识别；未启动 API 会话 |
| Codex | `codex --version` | `codex-cli 0.154.0` | CLI 可执行 |
| Codex | `codex agents --help` | 显示 shared app-server agent session 浏览命令 | 帮助路径可达；未在无认证交互会话中执行 spawn |
| Cursor | `command -v cursor` | 当前 shell 未发现命令 | 无 CLI 探针；由 adapter/unit/E2E 覆盖文件合同 |
| ZCode | `command -v zcode` | `unavailable` | 当前主机没有 ZCode CLI，无法完成设置页 Subagents smoke |
| OpenCode | `opencode --version` | `1.18.30` | CLI 可执行 |
| OpenCode | `opencode agent list --pure` | 返回 `build (primary)` 及其只读权限策略 | 只读 Agent 列表命令可达；未写入配置，也未把测试 Agent 注入用户环境 |

## 自动化替代验证

- `src-tauri/tests/phase8_e2e.rs` 新增 `agents_global_chain_covers_claude_codex_drift_readopt_delete_and_restore`：Claude Markdown 与 Codex TOML 覆盖 Preview → Apply → 外部漂移 → Readopt → 停用删除 → Restore。
- 后端 service/adapter/import 单测覆盖五工具 descriptor、OpenCode `mode: subagent`、Codex TOML 三字段、导入 `dropped_fields`、ZCode 项目级拒绝及 Codex trust 门禁。
- 前端测试覆盖 `/agents` CRUD/分配/状态展开/导入提示、项目 Agents 页签与 ZCode 排除、Dashboard/AppShell 能力集合。

## 限制与后续

本次环境缺少 ZCode，且没有为 Claude/Codex 启动会消耗认证或网络配额的真实会话；故“Claude `/agents` 列表可见”“Codex 新会话 spawn”“ZCode 设置页列出”“OpenCode Tab 不出现为主代理”四项不能在本机全部完成。待四个工具均可用并允许一次性隔离配置后，应使用应用生成的临时名称重复上述链路，并在结束时通过 Restore 删除。
