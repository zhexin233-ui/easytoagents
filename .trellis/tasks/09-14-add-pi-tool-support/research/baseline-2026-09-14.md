# 阶段 0 基线冻结（2026-09-14）

本文件是 implement.md 阶段 0 的只读基线记录，不改动 `verified-contract.md` 的既有结论。

## 0.1 基线质量门（动工前）

在动工前（添加 Pi 之前）于干净工作区运行：

| 命令                                              | 结果                                                                                                                                          |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 通过（lib 376 passed / 0 failed，e2e 与 bindings 全绿）                                                                                       |
| `pnpm typecheck`                                  | 通过                                                                                                                                          |
| `pnpm test --run`                                 | 通过（38 files / 329 tests）                                                                                                                  |
| `pnpm lint`                                       | **失败（预先存在）**：`eslint .` 命中未跟踪的 `.pi/extensions/trellis/index.ts`（不在 tsconfig project service 内）。与 Pi 接入无关，未修改。 |
| `pnpm format:check`                               | **失败（预先存在）**：Prettier 命中未跟踪的 `.pi/prompts/*.md` 与 `.pi/settings.json`（5 files）。与 Pi 接入无关，未修改。                    |
| `git diff --check`                                | 通过                                                                                                                                          |

> `.pi/` 是本机工具（Pi/Trellis 扩展）在工作区生成的未跟踪目录，未被 `.gitignore` 覆盖，
> 因此会让全仓库 lint/format 失败。它是环境产物而非本任务改动；阶段 6 的
> `pnpm check` 需要用户先决定是否把 `.pi/` 加入 ignore 或在提交前移除。

## 0.2 迁移基线

- 当前最新迁移：`src-tauri/src/db/migrations/0024_project_native_agent_files.sql`。
- 编译期 schema 版本断言：`src-tauri/src/app/mod.rs:595` `assert_eq!(version, 24)`。
- 本任务新增迁移文件名固定为 `0025_pi_tool_support.sql`（阶段 3 创建，本阶段未创建任何迁移）。

## 0.3 本机 Pi 事实（只读记录）

- `pi --version` → `0.85.1`；可执行文件 `/Users/zhexin/.volta/bin/pi`。
- 环境变量 `PI_CODING_AGENT_DIR` 未设置 → 默认 agent dir `~/.pi/agent`（与
  `verified-contract.md` §2 一致）。
- `~/.pi/agent` 布局（实际观察，节选与禁忌清单相关项）：
  - 只读探测输入：`settings.json`、`npm/node_modules/pi-mcp-adapter/package.json`。
  - **禁写**：`auth.json`、`models-store.json`、`trust.json`、`settings.json`、
    `extensions/`、`npm/`、`bin/`、`sessions/`、`run-history.jsonl`。
  - 受管目标存在性证据：`models.json`、`skills/`。
- `pi-mcp-adapter` 版本：`2.33.0`（`~/.pi/agent/npm/node_modules/pi-mcp-adapter/package.json`），
  与 `verified-contract.md` 的核验版本一致。
- `~/.pi/agent/settings.json` 的 `packages[]` 为字符串数组且包含 `npm:pi-mcp-adapter`，
  没有对象形 `extensions: []` 过滤 → 本机满足「已声明启用」。
- `~/.pi/agent/skills/` 中已有受管符号链接
  `smart-search-cli -> ~/Library/Application Support/com.easytoagents.desktop/skills/smart-search-cli`，
  即既有 EasyToAgents 中央库链接；印证 0.85.1 会跟随 skill 目录符号链接（与
  `research/prompt-and-skills.md` 的实机结论一致）。

## 0.4 结论

证据与 `research/verified-contract.md`、`research/pi-mcp-adapter.md` 的矩阵一致，
未发现需要回到规划的冲突。阶段 0 为纯只读操作（回滚点 R0：无代码变更）。
